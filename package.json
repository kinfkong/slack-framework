{
  "name": "slack-framework",
  "version": "1.0.0",
  "description": "This is the wrappers for slack APIs (including Slash Command)",
  "main": "index.js",
  "engines": {
    "node": "8"
  },
  "scripts": {
    "start": "cd template && node ./bin/www",
    "postinstall": "npm install ./template --no-save",
    "lint": "eslint . --ignore-pattern '**/node_modules/**' --ext .js || true"
  },
  "files": [
    "README.md",
    "index.js",
    "lib/"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.5.1",
    "lodash": "^4.17.4",
    "request": "^2.83.0",
    "winston": "^2.4.0"
  },
  "devDependencies": {
    "eslint": "^4.15.0",
    "eslint-config-topcoder": "^2.0.0"
  }
}
